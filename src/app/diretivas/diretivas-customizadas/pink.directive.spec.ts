import { PinkDirective } from './pink.directive';

describe('PinkDirective', () => {
  it('should create an instance', () => {
    const directive = new PinkDirective();
    expect(directive).toBeTruthy();
  });
});
